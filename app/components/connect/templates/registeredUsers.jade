div.row
  div.col-md-12.connect-helper
    div.row
      div.col-md-12
        h4.text-left Find your Honey
        p In order to get the most out of the honeydoo list, search for your better half and connect.
    div.row
      div.col-md-6
        h5.text-left Assign and share
        p The point is to connect, share, and organize your life and relationship. While your at it, earn points when you accomplish goals.
      div.col-md-6
        h5.text-left Search and get started
        p If your honey is already a registered user, use the search feature below. Find your significant other, and add them. It's that easy!
div.row
  div.col-md-8.col-md-offset-2.panel-container
    div.panel.panel-default
      div.panel-heading
        h5.text-center Search for your honey
      div.panel-body
        div.text-center(ng-show="disable")
          p You are currently connected, or have a pending connection status. Check your notifications.
          p To disconnect with you current honey, visit your account settings
            a(ui-sref="home.edit_profile") &nbsp;here.
        div.row()
          div.col-md-6.col-md-offset-3
            //div.form-group
            //  selectpicker(data-array="selectData", data-disable="disable", change-disable="changeDisable()", ng-model="myModel") {{myModel}}
            angucomplete-alt(id="searchHoney", placeholder="Honey Search", pause="200", selected-object="selectedUser", title-field="firstname,lastname", image-field="img", description-field="un", local-data="users", local-search="honeySearch", minlength="1", input-class="form-control", disable-input="disable")
            p {{selectedUser.description}}
      div.panel-footer(ng-cloak ng-hide="disable || !honey", ng-class="{'connected': !disable}")
        div.confirm-send-container.text-center
          p Confirm your invite to {{honey}}:
          button.btn(type="button", ng-click="!disable && setInviteStatus()") Send request!