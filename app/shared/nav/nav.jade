div.nav.navbar-custom.navbar-static-top
  div.container
    div.navbar-header
      button.navbar-toggle(type="button", data-toggle="collapse", data-target=".navbar-collapse")
        span.sr-only Toggle Navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      div.sidebar-toggle.pull-left(ng-hide="!loggedIn")
        input#unchecked.cbx.hidden(type="checkbox")
        label.lbl(for="unchecked", ng-click="changeSidenavState()")
      a.navbar-brand(href="index.html") Honeydoo List&nbsp;
        i.fa.fa-venus-mars
    div.collapse.navbar-collapse
      ul.nav.navbar-nav.navbar-right(ng-hide="loggedIn")
        li
          a(ng-click="showLogin()") Sign In
        li
          a(ng-click="showRegister()") Register
      ul.nav.navbar-nav.navbar-right.hidden
        li
          a(ui-sref="home.edit_profile") Account Settings
        li
          a(ng-click="logOut()") Sign Out
      ul.nav.navbar-nav.navbar-right(ng-hide="!loggedIn")
        li.dropdown
          a.dropdown-toggle(href="", data-toggle="dropdown")
            img(ng-src="{{user.password.profileImageURL}}", class="img-thumbnail")
            strong &nbsp;&nbsp;{{user.password.email}}&nbsp;&nbsp;
            i.fa.fa-angle-down
          ul.dropdown-menu
            li
              div.container-fluid
                div.row
                  div.col-sm-4
                    div.user-img-container
                      img.img-rounded(ng-src="{{user.password.profileImageURL}}")
                  div.col-sm-8.buttons-container
                    div.row
                      div.col-sm-12.acct-btn-container(ui-sref="home.edit_profile")
                        i.fa.fa-cogs
                        p.text-center &nbsp;&nbsp;Account Settings
                      div.col-sm-12.login-btn-container(ng-click="logOut()")
                        i.fa.fa-power-off
                        p.text-center &nbsp;&nbsp;Sign Out
